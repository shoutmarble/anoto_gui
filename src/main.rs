mod kornia;
mod bevy;

fn main() {
    println!("dots!");
    // Call the PDF generator
    // pdf::gen_anoto_pdf();
    // kornia::image_proc().unwrap();
    // ice::editor::my_counter().unwrap();
    bevy::bevy_hello();
}
